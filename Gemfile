source  'https://rubygems.org'

gem 'memcachier'                  # Dalli for Heroku environment
gem 'dalli'                       # Memcached client

gem 'puma'                        # Concurrent webserver
gem 'sinatra', '>= 1.4.0', require: 'sinatra/base'
gem 'rack-cache'                  # Caching middleware

# Debug
gem 'pry'
gem 'pry-doc'

group :test do
  gem 'rspec'
  gem 'simplecov' # Coverage tool
  gem 'rack-test'
end
